{
  "name": "smart-business-dashboard",
  "version": "1.0.0",
  "description": "Smart Business Dashboard - Backend API",
  "scripts": {
    "start": "node server/src/index.js",
    "build": "npm install --prefix server",
    "postinstall": "npm install --prefix server"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
